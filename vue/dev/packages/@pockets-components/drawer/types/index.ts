export type drawerAPI = {
  /**
      Provides an api for the drawer.
      @binding { reactive } drawer A reactive object that contains state for the drawer
      @binding { .open boolean } drawer whether the drawer is open  or not.
  */
  default:{}
}