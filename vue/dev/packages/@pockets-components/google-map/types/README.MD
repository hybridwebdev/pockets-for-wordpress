#Google Maps.TS

This file is pulled from the @types/google.maps on NPM. It's slightly modified so that
it can be used with defineProps for typecasting.

To update, simply pull the file from the repo, rename it to google-maps.ts and do a search
replace for ```declare namespace``` to ```export declare namespace```.